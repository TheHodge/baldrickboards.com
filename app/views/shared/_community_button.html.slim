/ Ask the community — always use this partial on light backgrounds (btn-line-light)
= link_to t('facebook_group_url'), target: "_blank", rel: "noopener", class: "btn btn-line-light" do
  = local_assigns.fetch(:label, 'Ask the community')
